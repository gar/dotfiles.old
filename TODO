* add other programs to install script
  - homebrew
  - rvm
  - vim
  - bash
  - tree
  - watch
  - vcprompt
* turn install script into Rakefile
* look into enabling the mail command on mac os x
* add ssh/config
* find and add the key-installer script
* look into the OpenTerminal app
* add more plugins to vim
* investigate folder name completion for ~/code directory
