* add other programs to install script
  - homebrew
  - rvm
  - vim
  - bash
  - tree
  - watch
  - vcprompt
* turn install script into Rakefile
* add ssh/config
* look into the OpenTerminal app
* investigate folder name completion for ~/code directory
* add a netcheck command
* add taks to update all vim bundles
* investigate bash autocompletion
* improve pg function to not include the grep
